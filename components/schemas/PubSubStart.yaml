type: object
properties:
  getproperties:
    $ref: ./GetProperties.yaml
  period:
    type: integer
  filter:
    type: array
    items:
      type: string
    example: '["networkProperties", "avgLatencyMs"]'
